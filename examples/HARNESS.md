# Project context

This is a Go web service. The codebase uses:
- Go 1.22 with standard library net/http (no frameworks)
- PostgreSQL via pgx
- Migrations in sql/migrations/

Run tests with `go test ./...`. Format with `gofmt`.
Do not add dependencies without asking first.
